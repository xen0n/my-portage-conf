# persistently pulling in openssl despite USE="ssl libressl"
www-servers/apache -ssl

# don't have access to github.com/gentoo/libressl atm
media-video/ffmpeg -openssl

# this is only about which SHA2 implementation to use
app-admin/sudo -openssl

# 20190413 libvncserver has no libressl support for now...
media-video/vlc -vnc

app-text/mupdf -openssl
